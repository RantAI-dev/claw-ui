# dependencies
/node_modules
/.pnp
.pnp.js

# next
/.next/
/out/
next-env.d.ts

# production
/build
/dist/

# env / secrets — never commit
.env
.env.local
.env*.local

# logs
*.log
/.gateway.log

# misc
.DS_Store
*.pem
.vercel
*.tsbuildinfo

# local dev gateway + runtime scratch (never commit)
/.devgateway/
/.shots/
*.pid
.devserver.log

# Nexus brand build dir
.next-nexus/
.devgateway/

# Internal working notes — audits and execution plans. Never commit: these are
# working artefacts, not product, and a set of them was once pushed to a public
# branch by accident. RantaiClaw's .gitignore carries the same two lines.
/plans/
/anti-slop/

# playwright
/test-results/
/playwright-report/
/blob-report/
/.playwright/
